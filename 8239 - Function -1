#include <bits/stdc++.h>
using namespace std;


int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    double n;
    while(cin >> n){
        double ans = (n*n*n) + 2*(n*n) - 3;
        
        cout << fixed << setprecision(4) << ans << '\n';
    }
}
